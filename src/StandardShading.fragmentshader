#version 330 core

// Output
out vec4 FragColor;     // Color of the fragment

// Input
in vec3 color;          // Color computed from the Vertex shader


void main()
{
    // Set the fragment color to the vertex shader output
    FragColor = vec4(color, 1.f);
}